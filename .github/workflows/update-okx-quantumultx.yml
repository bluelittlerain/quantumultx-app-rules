name: Update OKX Quantumult X rules

on:
  workflow_dispatch:
  schedule:
    - cron: "23 05 * * 3"

permissions:
  contents: write

concurrency:
  group: update-okx-quantumultx
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.13"

      - name: Run OKX unit tests
        run: python -m unittest discover -s tests -p "test_okx_rules.py" -v

      - name: Update approved OKX rules
        run: python scripts/update_okx_quantumultx.py --verbose

      - name: Validate OKX files
        run: python scripts/validate_okx_rules.py

      - name: Commit and safely push real changes
        shell: bash
        run: |
          if git diff --quiet -- \
            README.md \
            rule/QuantumultX/OKX/OKX.list \
            rule/QuantumultX/OKX/OKX-Web3.list \
            rule/QuantumultX/OKX/README.md; then
            echo "No approved OKX rule changes."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            README.md \
            rule/QuantumultX/OKX/OKX.list \
            rule/QuantumultX/OKX/OKX-Web3.list \
            rule/QuantumultX/OKX/README.md

          if git diff --cached --quiet; then
            echo "No generated OKX files to commit."
            exit 0
          fi

          git commit -m "chore: update OKX QuantumultX rules"
          branch="${{ github.event.repository.default_branch }}"
          for attempt in 1 2 3; do
            if git pull --rebase origin "$branch" && \
              git push origin "HEAD:$branch"; then
              exit 0
            fi
            git rebase --abort 2>/dev/null || true
            sleep $((attempt * 5))
          done
          echo "Unable to safely push after three attempts." >&2
          exit 1
